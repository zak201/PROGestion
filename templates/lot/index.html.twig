{% extends 'base.html.twig' %}

{% block title %}Lots{% endblock %}

{% block body %}
    <div class="container">
        <h1>Gestion des Lots</h1>
        <p>Ceci est la page où vous pourrez gérer vos lots.</p>

        {# Affichage de la pagination #}
        <div class="navigation">
            {{ knp_pagination_render(pagination) }}
        </div>

        {# Affichage des éléments #}
        {% for lot in pagination %}
            {# votre code pour afficher chaque lot #}
        {% endfor %}
    </div>
{% endblock %}
